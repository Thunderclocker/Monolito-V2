name: Carter Node minimum live regression

on:
  push:
    branches:
      - carter/node-min-22-6-stacked-20260905

permissions:
  contents: read

jobs:
  node-min-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Exercise install.sh version guard
        run: bash scripts/test_node_min_version.sh
